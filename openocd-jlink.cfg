# OpenOCD config for Solum EFR32BG22 tags via SEGGER J-Link (SWD).
#
# Wiring: GND, SWDIO, SWCLK, nRESET (optional but recommended), 3.3V tag power.

adapter driver jlink
transport select swd
adapter speed 4000

source [find target/efm32s2.cfg]
